# 🐦 Telegram Twitter Bot

Telegram-бот для просмотра твитов из X/Twitter в виде красивых карточек с поддержкой перевода, опросов, статистики и топиков.

## ✨ Возможности

- 📱 **Красивые карточки** в стиле FxTwitter/FixupX
- 🌍 **Перевод твитов** на разные языки (настраиваемый)
- 📊 **Опросы** с прогресс-барами
- 📈 **Статистика**: комментарии, репосты, лайки, просмотры
- 💬 **Цитируемые твиты** с форматированием
- 🖼️ **Медиа**: фото и видео (встроенно, не файлом)
- 🗂️ **Поддержка топиков** в супергруппах Telegram
- 🔒 **Защита**: rate limiting, whitelist пользователей
- 🐳 **Docker** для простого развёртывания

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo>
cd telegram-twitter-bot
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Минимальная конфигурация:

```env
BOT_TOKEN=your_bot_token_from_botfather
```

### 3. Запуск с Docker

```bash
docker compose up -d
```

Готово! Бот запущен и готов к работе.

### 4. Проверка логов

```bash
docker compose logs -f
```

## ⚙️ Конфигурация

Все настройки через переменные окружения в `.env`:

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `BOT_TOKEN` | Токен бота от @BotFather | **Обязательно** |
| `MODE` | Режим работы (polling/webhook) | `polling` |
| `TELEGRAM_USER_IDS` | Whitelist пользователей (через запятую) | Все пользователи |
| `REPLY_IN_GROUPS` | Отвечать в группах без упоминания (0/1) | `0` |
| `COMPRESS_MEDIA` | Сжимать медиа (0/1) | `1` |
| `MAX_MEDIA_MB` | Макс. размер медиа в МБ | `20` |
| `FX_BASE_URL` | URL альтернативного фронтенда | `https://fxtwitter.com` |
| `INCLUDE_QUOTED_MEDIA` | Показывать медиа в quoted твитах (0/1) | `0` |
| `DEFAULT_TRANSLATE_LANG` | Язык перевода по умолчанию | `off` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |

## 📝 Использование

### Команды бота

- `/start` — приветствие и описание
- `/help` — подробная справка
- `/translate <язык>` — включить перевод
- `/translate off` — выключить перевод
- `/translate status` — текущий статус перевода
- `/translate list` — список поддерживаемых языков
- `/settings` — показать все настройки

### Примеры

**Просто отправьте ссылку на твит:**

```
https://x.com/username/status/1234567890
```

**Включить перевод на русский:**

```
/translate Русский
```

или

```
/translate ru
```

**Поддерживаемые языки:**

- Русский (ru)
- English (en)
- Українська (uk)
- Español (es)
- Français (fr)
- Deutsch (de)
- Italiano (it)
- Português (pt)
- 日本語 (ja)
- 한국어 (ko)
- 中文 (zh)
- العربية (ar)
- Türkçe (tr)
- Polski (pl)
- Nederlands (nl)

### Работа в группах

**Личные сообщения:**
- Бот всегда отвечает на ссылки

**Группы:**
- По умолчанию отвечает только при упоминании: `@your_bot https://...`
- Можно включить автоответ через `REPLY_IN_GROUPS=1`

**Супергруппы с топиками (темами):**
- Бот автоматически отвечает в тот же топик, где была ссылка
- Передаёт `message_thread_id` во все сообщения

## 📦 Пример вывода

```
Elon Musk (@elonmusk) — 15.01.2025, 14:30

Making life multiplanetary 🚀

Переведено с English

Делаем жизнь межпланетной 🚀

💬 1.2K  🔁 5.3K  ❤️ 23K  👁 1.5M

Оригинал: открыть пост
```

**С опросом:**

```
Tech Poll (@techpoll) — 20.01.2025, 10:00

Какой язык программирования вы предпочитаете?

Python  45%  ████████████░░░░░░░░  (450)
JavaScript  30%  ██████░░░░░░░░░░░░░░  (300)
Go  15%  ███░░░░░░░░░░░░░░░░░  (150)
Rust  10%  ██░░░░░░░░░░░░░░░░░░  (100)

1 000 голосов · 2 дня до конца

💬 234  🔁 89  ❤️ 567  👁 12K

Оригинал: открыть пост
```

## 🧪 Тестирование

Запуск тестов:

```bash
# Установка зависимостей для разработки
pip install -r requirements.txt pytest

# Запуск тестов
pytest tests/ -v
```

Тесты покрывают:
- Нормализацию URL
- Извлечение ID твитов
- Форматирование чисел и прогресс-баров
- Парсинг опросов
- Форматирование карточек

## 🏗️ Структура проекта

```
telegram-twitter-bot/
├── src/
│   ├── bot.py              # Главный файл бота
│   ├── config.py           # Конфигурация
│   ├── handlers/           # Обработчики
│   │   ├── commands.py     # Команды (/start, /help, /translate)
│   │   └── messages.py     # Обработка сообщений с ссылками
│   ├── twitter/            # Работа с Twitter
│   │   ├── normalize.py    # Нормализация URL
│   │   ├── fetcher.py      # Получение данных через FxTwitter
│   │   ├── parser.py       # Парсинг HTML
│   │   ├── models.py       # Модели данных
│   │   └── translate.py    # Управление переводом
│   ├── media/              # Работа с медиа
│   │   ├── download.py     # Скачивание
│   │   ├── compress.py     # Сжатие
│   │   └── cleanup.py      # Очистка временных файлов
│   └── utils/              # Утилиты
│       ├── rate_limit.py   # Rate limiting
│       └── text_format.py  # Форматирование текста
├── tests/                  # Тесты
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── .env.example
└── README.md
```

## 🔧 Разработка

### Локальный запуск без Docker

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Настройка .env
cp .env.example .env
# Отредактируйте .env

# Запуск
python -m src.bot
```

### Сборка Docker образа

```bash
docker build -t telegram-twitter-bot .
```

## 🛡️ Безопасность

- ✅ Не хранит контент твитов (только настройки пользователей)
- ✅ Rate limiting (5 сек на пользователя, 3 сек на чат)
- ✅ Whitelist пользователей через `TELEGRAM_USER_IDS`
- ✅ Запуск от не-root пользователя в Docker
- ✅ Очистка временных файлов
- ✅ Таймауты на все HTTP запросы

## 📄 Лицензия

MIT License

## 🤝 Вклад

Буду рад pull request'ам и issues!

## ❓ FAQ

**Q: Почему бот не отвечает в группе?**

A: По умолчанию бот отвечает только при упоминании. Включите `REPLY_IN_GROUPS=1` или упоминайте бота: `@your_bot https://...`

**Q: Как работает перевод?**

A: Бот запрашивает переведённую версию через FxTwitter, добавляя язык к URL (например `/ru`). Если перевод недоступен, показывается оригинал.

**Q: Поддерживаются ли приватные твиты?**

A: Нет, бот работает только с публичными твитами через альтернативный фронтенд.

**Q: Можно ли изменить источник данных?**

A: Да, через `FX_BASE_URL` (например на `https://fixupx.com`)

**Q: Как бот работает с топиками?**

A: Автоматически! Бот определяет `message_thread_id` и отвечает в тот же топик.

## 📧 Контакты

Для вопросов и предложений создавайте issues в репозитории.