# Contributing to PMTwitter Bot

Спасибо за интерес к проекту! 🎉

## 📋 Правила контрибьюта

### 1. Создание Issue
Перед началом работы создайте issue с описанием:
- Что хотите добавить/исправить
- Зачем это нужно
- Как планируете реализовать

### 2. Fork & Branch
```bash
# Fork репозиторий на GitHub
# Клонируйте свой fork
git clone https://github.com/Pixmint/PmTwitter.git
cd pmtwitter

# Создайте ветку для фичи
git checkout -b feature/my-awesome-feature
```

### 3. Разработка

#### Структура проекта
- `src/handlers/` - обработчики команд и сообщений
- `src/twitter/` - работа с Twitter/X API
- `src/media/` - загрузка и обработка медиа
- `src/utils/` - утилиты

#### Код-стайл
```bash
# Используйте Black для форматирования
black src/

# Проверьте линтером
flake8 src/ --max-line-length=120

# Type checking
mypy src/
```

#### Тесты
```bash
# Запустите существующие тесты
pytest tests/ -v

# Добавьте тесты для нового функционала
# Пример: tests/test_my_feature.py
```

### 4. Коммит
```bash
# Осмысленные коммиты на русском или английском
git commit -m "Добавлена поддержка Twitter Spaces"

# Или английский
git commit -m "Add Twitter Spaces support"
```

Формат коммитов:
- `feat: новая функция`
- `fix: исправление бага`
- `docs: документация`
- `refactor: рефакторинг`
- `test: тесты`
- `chore: обновление зависимостей`

### 5. Pull Request

1. Обновите `CHANGELOG.md` в разделе `[Unreleased]`
2. Убедитесь что тесты проходят
3. Создайте PR с описанием изменений
4. Свяжите PR с issue (`Closes #123`)

#### Шаблон PR
```markdown
## Что изменено
- Добавлена фича X
- Исправлен баг Y

## Как тестировать
1. Запустить бота
2. Отправить команду /test
3. Проверить результат

## Checklist
- [ ] Код отформатирован (black)
- [ ] Тесты добавлены
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен
```

## 🐛 Reporting Bugs

При создании bug report укажите:
- Версию бота
- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Логи (без токенов!)

## 💡 Feature Requests

При предложении фичи опишите:
- Проблему которую она решает
- Как должна работать
- Примеры использования

## ⚙️ Настройка окружения

```bash
# Python 3.12+
python3 -m venv venv
source venv/bin/activate

# Зависимости
pip install -r requirements.txt
pip install black flake8 mypy pytest

# .env файл
cp .env.example .env
nano .env  # Добавьте BOT_TOKEN

# Запуск
python -m src.bot
```

## 📝 Документация

При изменении API обновите:
- README.md (если меняется использование)
- Docstrings в коде
- .env.example (новые переменные)

## 🔒 Безопасность

- Не коммитьте токены и секреты
- Используйте `.env` для локальной разработки
- Проверяйте `.gitignore`

## 📞 Вопросы?

- Issues: [GitHub Issues](https://github.com/Pixmint/PmTwitter/issues)

---

**Спасибо за вклад в проект!** ❤️
